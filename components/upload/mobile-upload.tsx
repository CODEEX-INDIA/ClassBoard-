"use client";
import { useState } from "react";
import { importDocument } from "../../lib/importers";
export function MobileUpload({ sessionId }: { sessionId: string }) { const [message, setMessage] = useState("Choose a lesson file to send securely."); return <main className="mobile-upload"><h1>Send to Smartboard</h1><p>{message}</p><label className="primary">Choose PDF, PPT, or PPTX<input hidden type="file" accept=".pdf,.ppt,.pptx,application/pdf,application/vnd.ms-powerpoint,application/vnd.openxmlformats-officedocument.presentationml.presentation" onChange={async e => { const file = e.target.files?.[0]; if (!file) return; setMessage("Preparing upload…"); try { await importDocument(file); setMessage(`Ready to send ${file.name}. Connect Supabase to deliver it to session ${sessionId}.`); } catch (error) { setMessage(error instanceof Error ? error.message : "Upload failed."); } }} /></label><small>Session expires automatically. No credentials are placed in the QR link.</small></main>; }
